# ======================== General Dependencies/Build Artifacts ========================
# Node modules for all packages (managed uniformly by pnpm, no need to commit)
node_modules/
# Build artifacts for all packages (dist directories of sub-packages in monorepo)
**/dist/
**/build/
**/out/
# Vite temporary cache
**/.vite/
# TypeScript compilation artifacts
**/*.tsbuildinfo
**/lib/
**/es/

# ======================== Environment Variables/Sensitive Configurations ========================
# Environment variable files (contain secrets, API addresses, etc., keep locally only)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Private keys/certificates
*.pem
*.key
*.cert

# ======================== pnpm Specific ========================
# pnpm global storage (local cache, no need to commit)
.pnpm-store/
# pnpm workspace temporary files
pnpm-debug.log*
# Note: pnpm-lock.yaml MUST be committed! (Lock dependency versions to ensure consistent team environments)

# ======================== Git/System ========================
# Git temporary files
.git/
.gitignore.local
# macOS system files
.DS_Store
# Windows system files
Thumbs.db
desktop.ini
# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ======================== Editors/IDEs ========================
# VSCode
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
# WebStorm/IntelliJ
.idea/
*.iml
*.ipr
*.iws
# Vim
*.swp
*.swo
# Sublime Text
*.sublime-project
*.sublime-workspace

# ======================== Other Temporary Files ========================
# Cache files
.cache/
.tmp/
# Compressed packages
*.zip
*.tar.gz
*.7z
# Docker temporary files (ignore if testing Docker locally, no need to commit)
.dockerignore
Dockerfile.local

# ======================== Claude Code ========================
# Claude Code related files
.claude/
*.claude